'use strict';

/**
 * product-usage-history router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::product-usage-history.product-usage-history');
